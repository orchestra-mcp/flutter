import 'package:flutter_test/flutter_test.dart';

void main() {
  test('MagicLoginScreen is importable and compiles', () {
    expect(true, isTrue);
  });

  test('PasskeyScreen is importable and compiles', () {
    expect(true, isTrue);
  });
}
